System 8 – Business Rule + AI Hybrid Engine

Combines deterministic business rules with AI reasoning.
Rules are applied first; AI is used only when rules do not match.

Ensures safety, predictability, and explainability.
